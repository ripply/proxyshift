<%= simple_form_for User.new, :url => users_path do |f| %>
  <%= f.input :account, label: 'Account name' %>
  <%= f.input :email, label: 'Email address' %>
  <%= f.input :password %>
  <%= f.association :type %>
  <%= f.button :submit %>
<% end %>