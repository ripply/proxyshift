{
    "push": {
        "gcm": {
            // contains actual api key
            "serverapikey": "PUSH_GCM_APIKEY"
        },
        "apn": {
            // contains actual certificate or path to it
            // defaults to cert.cer
            "cert": "PUSH_APN_CERT",
            // contains actual key or path to is
            // defaults to key.cer
            "key": "PUSH_APN_KEY"
        }
    }
}
