<div class="row text-center">
    <div class="col-md-8 col-md-offset-2">
        <div class="section-title center">
            <h1>{{_ 'unsubscribe.email.success.message' }}</h1>
            <h4>{{_ 'unsubscribe.email.success.undo' }}</h4>
        </div>
    </div>
</div>
